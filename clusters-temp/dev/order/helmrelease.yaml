apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: order
  namespace: default
spec:
  interval: 1m
  releaseName: order
  chart:
    spec:
      chart: order
      version: "0.1.0"
      sourceRef:
        kind: HelmRepository
        name: online-store-repo
        namespace: flux-system
